define(function () {
    "use strict";

    return {};
    /*var rootScope;

    function create() {
        var scopeObj = new Object();
        return scopeObj;
    }

    function getScope() {
        if (!rootScope) {
            rootScope = create();
        }
        return rootScope;
    }

    return getScope();*/
});