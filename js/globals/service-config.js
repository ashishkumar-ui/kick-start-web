define(function (require) {
    "use strict";

    return {
        global: {
            getProducts: "products.json"
        },
        testModule: {
            getPrograms: "program-list.json"
        }
    };
});